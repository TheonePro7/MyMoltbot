# AGENTS.md

## Cursor Cloud specific instructions

This repository (`MyMoltbot`) is currently an empty scaffold with only a `README.md`.

### Current state
- No source code, frameworks, or dependencies exist yet.
- No build, lint, or test commands are available.
- The repo has a single commit on `main` with just the README.

### When source code is added
- Update this section with the chosen tech stack, how to install dependencies, and how to run/test/lint/build.
- Update the VM environment update script accordingly (via `SetupVmEnvironment`).

### Environment
- Node.js v22 and Python 3.12 are available in the VM.
- Git 2.43 is pre-installed.
